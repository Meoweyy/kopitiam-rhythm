{
  "name": "@kopitiam/core",
  "version": "0.1.0",
  "private": true,
  "description": "Pure measurement core of the Kopitiam Rhythm instrument: ports, protocol constants, session/block engine, pattern generation, adaptive policies and timing analysis. Deliberately free of React Native, the DOM and Node APIs.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint ."
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^3.0.0",
    "@types/node": "^22.10.0"
  }
}
