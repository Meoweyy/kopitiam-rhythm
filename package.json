{
  "name": "kopitiam-rhythm",
  "version": "0.1.0",
  "private": true,
  "description": "A self-administered tablet rhythm game that trains the internal clock in older adults by progressively withdrawing the pacing cue. Final Year Project — randomised two-arm trial.",
  "workspaces": [
    "app",
    "packages/*"
  ],
  "engines": {
    "node": ">= 22.11.0"
  },
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "test:core": "npm run test --workspace @kopitiam/core",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "verify": "npm run typecheck && npm run lint && npm run test",
    "android": "npm run android --workspace KopitiamRhythm",
    "start": "npm run start --workspace KopitiamRhythm"
  }
}
